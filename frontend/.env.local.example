# Copy to .env.local — Next.js reads NEXT_PUBLIC_* vars from here at build/dev time.
# Point at the Pi's IP when running on venue WiFi; localhost is fine for local dev.
NEXT_PUBLIC_REWIND_API=http://localhost:8000
